Question,Answer
When does useLayoutEffect run?,After DOM mutations but before the browser paints.
When does useEffect run?,After the browser has painted.
Why use useLayoutEffect for DOM measurements?,To ensure measurements and updates happen before the user sees the UI, preventing layout jumps.
What happens if you measure DOM in useEffect?,You may see a flash of unmeasured or incorrect layout before the effect runs.
Give a use case for useLayoutEffect.,Measuring element size, synchronizing scroll position, triggering animations before paint.
What is a performance risk of overusing useLayoutEffect?,It can block the browser's paint and make the UI less responsive.
When should you use useEffect instead?,For side effects that don't affect layout or need to block paint (e.g., data fetching, logging).