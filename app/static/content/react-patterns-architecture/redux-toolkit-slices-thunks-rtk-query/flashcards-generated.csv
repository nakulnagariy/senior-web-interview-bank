Question,Answer
What is a slice in Redux Toolkit?,A collection of state, reducers, and actions for a feature, created with createSlice.
How does createAsyncThunk simplify async logic?,It generates pending/fulfilled/rejected actions and handles promise lifecycles automatically.
What is RTK Query?,A data fetching and caching tool built into Redux Toolkit that generates endpoints and hooks.
How does RTK Query differ from manual thunks?,It manages cache, deduplication, and background updates, reducing boilerplate.
Give an example of a slice.,const todosSlice = createSlice({ name: 'todos', initialState: [], reducers: { addTodo: ... } });
Give an example of a thunk.,export const fetchTodos = createAsyncThunk('todos/fetch', async () => { ... });
Give an example of an RTK Query endpoint.,const api = createApi({ endpoints: builder => ({ getTodos: builder.query({ query: () => 'todos' }) }) });
What is a trade-off of RTK Query?,Less boilerplate but more abstraction; may not fit all custom logic.
When should you use slices and thunks vs RTK Query?,Use slices/thunks for complex state logic; use RTK Query for standard data fetching needs.